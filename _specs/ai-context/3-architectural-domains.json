{
  "data-layer": {
    "required_patterns": {
      "json-schema-validation": "All Bible data must conform to JSON Schema Draft-07 definitions. Schemas define strict structures with $ref references between related schemas.",
      "content-schema-recursion": "The content-schema.json uses recursive $ref to allow arbitrarily nested content structures (strings, objects, or arrays of content).",
      "book-identifiers": "Books use 3-character uppercase identifiers (e.g., 'GEN', 'MAT', 'REV') that must match entries in bible-books.json.",
      "version-book-ordering": "Each Bible version maintains its own book ordering via the 'order' property (1-indexed, sequential, no gaps).",
      "verse-file-naming": "Verse files follow pattern '{order}-{bookId}.json' (e.g., '01-GEN.json', '40-MAT.json').",
      "ajv-validation": "Use AJV library for JSON Schema validation with support for $ref resolution across schema files.",
      "atomic-file-writes": "Write JSON output via writeJsonFile() and raw text (Markdown, pre-formatted JSON) via writeFileAtomic(), both from functions/writeJsonFile.ts. Do not call fs.writeFileSync directly for pipeline output. Both helpers stage bytes to a temp file and rename over the target, retrying on a backoff if the rename is transiently blocked.",
      "json-canonicalization-from-parsed-data": "Any JSON formatting pass (writeJsonFile()'s formatJsonData(), and validate.ts's own reformat-on-change check) must derive its output from JSON.parse'd data, never from a file's existing raw text. Prettier preserves whichever line breaks its input already has rather than re-deciding them from width, so formatting raw text lets a stray break (e.g. from a prior bug, or a different tool's output style) persist across every future validate run instead of self-correcting.",
      "cross-chapter-bible-link-splitting": "A bibleLink target whose range spans two chapters of the same book must be split into two chapter-scoped links joined by a literal en dash. This applies whether the range names a verse on each end (e.g. '2 Kings 6:31–7:20') or only a chapter on each end (e.g. 'Romans 1–11'). A target cut off short of the multi-verse range its own display text already names must be completed first, and a target whose book resolves within the version's own canon but names a chapter/verse that version doesn't carry must be unlinked, keeping its display text as plain content — a book that fails to resolve within canon at all is left alone rather than unlinked, since a footnote can legitimately cite a book its own version doesn't carry. All three rules live in utils/crossChapterLinks.ts, which has no CLI or standalone script of its own — utils/validate.ts is the only caller, running each fixer as a step in its own auto-fix pass and each finder as one of its report-only audits.",
      "strong-node-text-flow-conventions": "A joining space belongs on the leading edge of the node it joins, never the trailing edge of the node before it — enforced for both strong and foot; a connector word left unmerged should fold forward into the strong-carrying neighbor after it, never backward; a verse's own outermost content must never open with whitespace; a heading or subtitle run must be followed by a node carrying paragraph: true. Audited by utils/auditNodes.ts across many distinct drift shapes and run per version from within utils/validate.ts — auditNodes.ts itself is read-only and has no CLI, though several of the checks each have a separate fixer (under utils/ or functions/) that reuses the audit's own eligibility functions rather than re-deriving the judgment, called unconditionally as a step in validate.ts's own auto-fix pass.",
      "no-meaningless-content-nodes": "A node must never carry only formatting (marks/script) with no text to apply it to, nor an empty '' text husk left over from stripped marks. Enforced by utils/validate.ts's findMeaninglessContentNodes()."
    },
    "architectural_constraints": {
      "no-database": "All data stored as flat JSON files, no traditional database. Read/write via Node.js fs module.",
      "schema-reference-urls": "Schema $id and $ref use GitHub URLs but resolve locally via AJV schema registration.",
      "immutable-source-data": "Bible verse JSON files in version directories are source data; exports go to separate exports/ directory.",
      "content-flexibility": "Content can be plain string, structured object with metadata, or array combining both. Code must handle all cases.",
      "content-nested-carries-marks": "A ContentNested wrapper ({content, strong, ...}) may itself carry marks (bold/italic/woc/small-caps applying to an entire nested subtree), not just leaf ContentObject nodes.",
      "strong-number-format": "Strong's numbers must match pattern '^[GH][0-9]{1,4}$' (G for Greek, H for Hebrew).",
      "version-scoped-chapter-lengths": "A chapter's last verse must be read from that version's own verse records, never a shared or borrowed table. Versification (e.g. Romans 14's last verse) differs between translations.",
      "single-footnote-per-node": "The schema allows only one foot per node; a word's second footnote must ride as a textless sibling node immediately after it, carrying only foot."
    }
  },
  "ui": {
    "required_patterns": {
      "react-hooks": "Use React 18 hooks (useState, useEffect, useMemo, useRef) for all state and lifecycle management.",
      "babel-in-browser": "JSX transpiled at runtime via Babel standalone. No build step for frontend.",
      "cdn-dependencies": "React, ReactDOM, Tailwind CSS, Font Awesome, and fonts loaded via CDN in index.html.",
      "component-registration": "Each component file assigns to window object (e.g., 'window.App = App') for cross-file access.",
      "tailwind-utility-classes": "Use Tailwind CSS utility classes for styling. Dark mode via 'dark:' prefix variants.",
      "script-specific-fonts": "Hebrew text uses 'David Libre' font with RTL direction; Greek uses 'EB Garamond'; Latin uses 'Atkinson Hyperlegible'.",
      "shared-footnote-text-extraction": "Footnote display text must be derived through the single shared window.getFootnoteText (web/public/js/footnoteText.js), not re-flattened inline per rendering path. It recurses through every foot.content shape (string, array, {bibleLink, content?}, {text}) so every footnote flattens identically regardless of which node shape it's attached to."
    },
    "architectural_constraints": {
      "no-build-process": "Frontend has no webpack/vite/rollup. All JS loaded directly via script tags with Babel transpilation.",
      "single-page-app": "Single index.html entry point. All navigation is in-memory state changes.",
      "prop-drilling": "State flows from App component down via props. No Context API or global state.",
      "component-order": "Script tags in index.html must load in dependency order (Icons → Toggle → ... → App); footnoteText.js must load before ContentNode.js, since it's a window-global dependency, not a module import.",
      "dark-mode-class": "Dark mode toggled via 'dark' class on document.documentElement."
    }
  },
  "routing": {
    "required_patterns": {
      "api-prefix": "All API endpoints use '/api/' prefix (e.g., '/api/versions', '/api/books', '/api/content/:version/:bookId').",
      "static-public-dir": "Static files served from 'web/public/' directory. Root '/' serves index.html.",
      "content-endpoint-pattern": "Bible content fetched via '/api/content/{versionId}/{bookId}' returning array of verse objects."
    },
    "architectural_constraints": {
      "no-spa-routing": "No client-side routing library. Navigation is state-based within single page.",
      "no-framework-server": "Simple Node.js http server without Express or other frameworks.",
      "directory-traversal-prevention": "Server checks that resolved file paths start with PUBLIC_DIR to prevent traversal attacks.",
      "mime-type-mapping": "Server maps file extensions to MIME types for proper content-type headers."
    }
  },
  "state-management": {
    "required_patterns": {
      "centralized-app-state": "All application state lives in App component: versions, selections, content, settings, UI state.",
      "derived-state-memoization": "Use useMemo for computed values (currentVersion, availableBooks, currentBook, chapterContent).",
      "settings-object": "Settings stored as single object with keys for all display options (paragraphMode, showStrongs, darkMode, etc.).",
      "toggle-pattern": "Boolean settings toggled via toggleSetting function that spreads previous state."
    },
    "architectural_constraints": {
      "no-persistence": "Settings and state not persisted to localStorage. Reset on page reload.",
      "no-async-state": "No async state management (no React Query, SWR). Simple fetch in useEffect.",
      "loading-state": "Loading boolean tracks API request state; content cleared on error."
    }
  },
  "usfm-import": {
    "required_patterns": {
      "tokenize-then-segment": "utils/usfm/tokenize.ts lexes raw USFM into a flat, source-ordered token stream; utils/usfm/segmentVerses.ts is the only module that decides paragraph/stanza/chapter boundaries from that stream. Specialized builders (headings.ts, footnotes.ts, references.ts, inlineMarks.ts) render spans segmentVerses.ts has already identified; they don't make their own boundary decisions.",
      "shared-footnote-classification-table": "utils/usfm/footnoteTypeRules.ts's classifyFootnote() is the single source of truth for footnote type. Both the importer (via footnotes.ts) and the standalone retroactive tool (utils/overhaulFootnotes.ts) call this same function, so the two entry points can never disagree about a footnote's type. Rules classify by construct (citation-only, names a witness, opens with a translation marker, compares languages) rather than by phrases memorized from one edition's own house style, so new signal patterns should generalize across translations rather than being added as edition-specific literals.",
      "shared-embedded-reference-scanner": "utils/usfm/references.ts's linkEmbeddedReferences() is the single source of truth for finding a Scripture reference sitting in ordinary footnote prose with no \\x marker. Both the importer (via footnotes.ts, at parse time) and the standalone retroactive tool (utils/overhaulReferences.ts, against already-built bible-versions/**/*.json) call this same function, so the two entry points can never disagree about what resolves. This scan is deliberately never canon-restricted — an embedded mention names a real book regardless of whether the version being read carries it — unlike an explicit \\x cross-reference or a direct-branch prose match, which both resolve only within the target version's own canon.",
      "subprocess-regeneration-after-write": "A real (non-preview, non-redirected) import run in utils/importUsfm.ts shells out to `npm run validate` as a child process rather than calling its exported functions in-process, specifically so crossChapterLinks.ts's own per-process cached book index can't go stale mid-import. There is no separate audit-links step; validate.ts owns every normalization and audit rule this repo enforces."
    },
    "architectural_constraints": {
      "verify-must-not-share-parsing-with-importer": "utils/usfm/verify.ts must never import tokenize.ts, segmentVerses.ts, or anything that imports them. Its entire purpose is catching a bug the importer itself couldn't see, which requires deriving its own verse/chapter/footnote/marker counts independently from raw USFM.",
      "no-package-json-script-for-import": "utils/importUsfm.ts is deliberately excluded from package.json's scripts. It's invoked directly via ts-node with a source directory that varies per translation; its own header comment is the documented invocation.",
      "strongs-suppression-is-per-version": "--no-strongs (ImportOptions.strongs = false) is a deliberate, translation-specific decision based on a quality review of that translation's Strong's-number alignment, not a default to toggle without redoing that review.",
      "webus2020-corpus-is-gitignored": "utils/usfm/headings.ts, footnotes.ts, and verify.ts import their Hebrew/Greek run-splitting helper from the tracked utils/usfm/splitScriptRuns.ts, not from a gitignored imports/_lib/ path as earlier versions of this pipeline did. The WEBUS2020 raw USFM corpus at imports/webus2020/ebible-usfm/ is still gitignored, but no test in this domain reads it anymore: metadata.test.ts, verify.test.ts, and the upstream-convention specs were rewritten against tracked .usfm fixtures or against this repo's own committed HEAD content instead. New tests added to this domain should keep that property — a gitignored local corpus must never become something npx vitest run needs to pass. See 6-tests-and-build.md for current test-suite status.",
      "footnote-overhaul-downgrades-to-stu-only-on-hard-reset": "utils/overhaulFootnotes.ts's reclassifyFootnotesIn() must skip a recomputed 'stu' result whenever the footnote's stored type is a different real type, unless --hard-reset was given. 'stu' is classifyFootnote()'s own no-signal fallback, not a considered judgment, so overwriting a stored trn/var/xrf with it on no textual evidence would discard a real prior classification rather than correct one. Every upgrade out of 'stu' stays unconditional either way. --hard-reset gives the stored type no weight at all, for a corpus being reclassified from scratch rather than corrected; without it a stored non-stu type is unreachable by stu."
    }
  },
  "export-system": {
    "required_patterns": {
      "dual-export-formats": "Export to both text-vbv-strongs (annotated text) and markdown-par (paragraph markdown).",
      "recursive-content-processing": "Content processing functions must handle string, object, and array recursively.",
      "footnote-lettering": "Footnotes labeled with letters (a, b, c...) cycling after 'z', per chapter.",
      "chapter-grouping": "Markdown export groups verses by chapter with ## headers.",
      "shared-emphasis-delimiter-spans": "Adjacent array siblings carrying the same open bold/italic (b/i) marks must render under one shared open/close delimiter pair, not one pair per sibling. This is tracked as an open/close transition between consecutive items, not per-item wrapping. Per-item wrapping produces broken markdown like '**word****word**' instead of '**word word**'."
    },
    "architectural_constraints": {
      "version-directory-mirroring": "Export directories mirror version directory structure.",
      "cli-arguments": "Export script accepts optional version and bookId arguments for selective export.",
      "main-module-check": "Export only runs when script is main module (require.main === module)."
    }
  }
}
